## Packages
framer-motion | Smooth animations for page transitions and card interactions
lucide-react | Premium iconography (already in base but ensuring version match)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
The backend provides an image generation endpoint at GET /api/welcome
We will display the generated image using a standard <img> tag with query parameters.
No complex auth required for this playground.
